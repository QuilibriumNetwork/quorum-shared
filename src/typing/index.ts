/**
 * Typing Module
 *
 * Ephemeral typing-indicator signaling. Platform-agnostic service that
 * relies on caller-supplied encrypt/transport callbacks and a privacy gate.
 */

export { TypingService } from './service';
export type { TypingServiceOptions } from './service';
